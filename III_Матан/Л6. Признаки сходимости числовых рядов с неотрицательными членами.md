*🚨🚨 В материале могут быть опечатки и ошибки 🚨🚨*

*Новоженов Павел*
*ЭН-26*

---

## Признак сравнения
Пусть даны два ряда 
$$1) \ \sum_{k=1}^\infty a_k$$
$$2) \ \sum_{k=1}^\infty b_k$$

Если ряд $2$ сходится и выполняется неравенство $a_k \leq b_k$, то ряд $1$ также сходится. Если ряд $1$ расходится, то ряд $2$ также расходится.

Доказательство:
Пусть ряд $2$ сходится. Обозначим частичную сумму ряда $2$ через $\sigma_n = \sum_{k=1}^n b_k$. Если ряд сходится, то существует $\lim_{n\rightarrow\infty}\sigma_n$

Обозначим через $S_n = \sum_{k=1}^n a_k$. Составим последовательность частичных сумм $S_1, \ S_2, \ S_3, ...$. Это последовательность является неубывающей.

$$S_n \leq \sigma_n \ - \ по \ условию \ теоремы \ (3)$$

Так как $\lim_{n\rightarrow\infty}\sigma_n = \sigma$, то $S_n \leq \sigma$. 
$$\lim_{n \rightarrow \infty} S_n - \ сходится$$

Пусть $\sum_{k=1}^\infty a_k$ расходится. Тогда можно записать, что
$$\lim_{n\rightarrow\infty} S_n = \infty$$

Учитывая неравенство $(3)$ можно сказать, что и $\sigma_n$ будет возрастать до бесконечности.

## Предельный признак сравнения рядов с положительными членами
Пусть имеются два ряда $\sum_{k=1}^\infty a_k, a_k > 0$ и $\sum_{k=1}^\infty b_k, b_k > 0$.
Если $\lim_{k \rightarrow\infty}\frac{a_k}{b_k} = A$, где $A$ - конечное число. Тогда оба ряда ведут себя одинаково. Одновременно сходятся или расходятся.

Доказательство:
Пусть $\lim_{k\rightarrow\infty}\frac{a_k}{b_k} = A$. По определению это значит, что справедливо неравенство:
$$|\frac{a_k}{b_k} - A|<\varepsilon, \ k > N(\varepsilon) \ (4)$$
$$-\varepsilon < \frac{a_k}{b_k} - A < \varepsilon \ (5)$$

Пусть ряд $\sum_{k=1}^\infty b_k$ сходится. Воспользуемся правой частью неравенства $5$.
$$\frac{a_k}{b_k} - A < \varepsilon \ (6)$$
$$\frac{a_k}{b_k} < A + \varepsilon, \ k > N(\varepsilon) \ (7)$$
Так как $b_k > 0$
$$a_k < (A+\varepsilon)b_k \ (8)$$

Так как ряд $\sum_{k=1}^\infty b_k$ сходится, то сходятся и ряд $\sum_{k=1}^\infty (A+\varepsilon)b_k$. Отсюда следует, что сходится и ряд $\sum_{k=1}^\infty a_k$ по признаку сравнения.

Пусть ряд $\sum_{k=1}^\infty a_k < \infty$, то есть ряд сходится. Воспользуемся правой частью неравенства.

$$\frac{a_k}{b_k} - A > - \varepsilon \ (10)$$
$$\frac{a_k}{b_k} > A - \varepsilon \ (11)$$
$$a_k > (A - \varepsilon) b_k \ (12)$$

Аналогично ряды $\sum_{k=1}^\infty a_k$ и $\sum_{k=1}^\infty b_k$ сходятся.


## Достаточные признаки сходимости числовых рядов
### Признак Даламбера
Пусть дан числовой ряд $\sum_{k=1}^\infty a_k, \ a_k > 0$. Если $\lim_{}\frac{a_{k+1}}{a_k} = q < 1$, ряд сходится, $q > 1$ расходится, $q = 1$ требуется дополнительное исследование. 

Если получили единицу то надо проверить в первую очередь необходимые условия ([[Л5. Числовые ряды#Критерий Вейерштрасса сходимости числовых рядов|Критерий Вейерштрасса]]).

> Признак Даламбера полезно применять там, где ряд содержит факториал.

Доказательство. 
Пусть $\lim_{k\rightarrow\infty}\frac{a_{k+1}}{a_k} = 1$ и $q < 0$.
$$|\frac{a_{k+1}}{a_k} - q| < \varepsilon \ (13)$$
$$ - \varepsilon < |\frac{a_{k+1}}{a_k} - q| < \varepsilon \ (14)$$

$$\frac{a_{k+1}}{a_k} - q < \varepsilon \ (15)$$
$$\varepsilon = r - q, \ \ r\in(q, 1)$$
$$\frac{a_{k+1}}{a_k} - q < r-q$$
$$\frac{a_{k+1}}{a_k} < r \ (16)$$

$$a_{k+1} < r a_k \ (17)$$

$$k = N_1, a_{N_1 + 1} < ra_{N_1} < r^2 a_{N_1}$$
$$k = N_1 + 1, a_{N_1 + 2} < ra_{N_1 + 1} < r^3 a_{N_1}$$

$$a_1 + a_2 + \dots + a_{N_1} + a_{N_1 + 1} + \dots (18)$$
$$a_{N_1} + ra_{N_1} + r^2a_{N_1} + \dots  \ (19)$$

Тогда можно утверждать, что ряды $(18)$ и $(19)$ сходятся по признакам сравнения.

Пусть $r > 1$. 
$$ - \varepsilon < |\frac{a_{k+1}}{a_k} - q| < \varepsilon \ (14)$$
$$-(q-r) < \frac{a_{k+1}}{a_k} - q$$
$$a_{k+1} > ra_k$$
$$a_{N_1 + k} > r^ka_{N_1} (20)$$
$$\lim_{k\rightarrow\infty a_{N_1+k}} = \infty - \ ряд \ расходится$$


### Радикальный признак Коши
Пусть дан числовой ряд $\sum_{k=1}^\infty a_k, a_k > 0$. Если существует $\lim_{k\rightarrow\infty} \sqrt[k]{a_k} = q$. Если $q < 1$ - ряд сходится, $q > 1$ расходится, $q = 1$ требуется доп исследование.

Радикальный признак Коши удобно использовать, когда ряд представлен степенями. При этом учитывать два важных предела:
$$lim_{k \rightarrow \infty} \sqrt[k]C = 1, \ C > 0$$
$$lim_{k \rightarrow \infty} \sqrt[k]{k^\alpha} = 1, \ \alpha  \ не \ зависит \ от \ k$$

Доказательство аналогично доказательству признака Даламбера.

### Интегральный признак Коши
Пусть дан числовой ряд $\sum_{k=1}^\infty a_k, \ a_k \geq 0$. Пусть $f(x)$ непрерывная функция при $x \geq 1, \ f(x) \geq 0, \ f(n)=a_n$. Тогда $\int_1^\infty f(xdx)$ и $\sum_{k=1}^\infty a_k$ сходятся и расходятся одновременно.

Докажем с помощью геометрии. Выделим выступающие треугольники, найдем их площадь.
$$S = a_1 + a_1 + \dots + a_{n} = \int_1^{n+1}f(x)dx \ (1)$$
$$S = a_2 + a_3 + \dots + a_{n+1} = S_{n-1}-a_1 \ (2)$$
$$S_{n+1}-a_1 < \int_1^{n+1}f(x)dx \ (3)$$
$$S_{n+1}-a_1 < \int_1^{\infty}f(x)dx \ (4)$$

Предположим, что $\int_1^{\infty}f(x)dx$ существует. Так как $\int_1^{n+1}f(x)dx < L$, то $S_{n+1} < a_1 + L$
$$S_1 < S_2 < \dots < a_1 + L \ (7)$$

Из 7 следует, $\lim_{n\rightarrow \infty} S_n = S$ , то есть ряд сходится.

Пусть интеграл $\int_1^{\infty}f(x)dx$ расходится. Тогда из неравенства 1 следует, что ряд расходится.